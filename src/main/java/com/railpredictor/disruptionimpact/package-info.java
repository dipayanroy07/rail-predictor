/** The policy boundary between real/mock railway operational disruption data ({@code
 * railwaydisruption}) and predicted delay impact (Phase 19). See {@code DisruptionImpactPolicy}'s
 * own Javadoc for the exact semantics, and {@code HeuristicDisruptionImpactPolicy}'s for which
 * numbers are empirically grounded versus provisional heuristics. */
package com.railpredictor.disruptionimpact;
