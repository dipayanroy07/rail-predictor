package com.railpredictor.model.enums;

/** General weather condition, independent of any specific weather API's own vocabulary. */
public enum WeatherCondition {
    CLEAR,
    RAIN,
    HEAVY_RAIN,
    FOG,
    DENSE_FOG,
    UNKNOWN
}
